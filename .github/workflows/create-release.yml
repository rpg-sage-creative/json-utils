name: create release

on:
  push:
    tags:
      - v*

jobs:
  release:
    name: Release pushed tags
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Create release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          tag: ${{ github.ref_name }}
        run: |
          gh release create "$tag" \
              --repo="$GITHUB_REPOSITORY" \
              --title="${GITHUB_REPOSITORY#*/} ${tag#v}" \
              --generate-notes

  call-workflow:
    runs-on: ubuntu-latest
    steps:
      - name: Call release package
      - uses: rpg-sage-creative/$GITHUB_REPOSITORY/.github/workflows/release-package.yml@main
